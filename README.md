YOGI PATEL - 171378 
OOP_Foundations Assignment 


1. A class is like a blueprint or template for creating objects . In a linked list . For example , one class would define the structure of a Node ( the data and the pointer ) . Another class would define the LinkedList itself to handle things like adding or searching for data .

2. A node is a single element of the structure that holds data and a pointer to the next node. This entire collection or chain of such nodes linked together to form a complete data structure is called a linked list.

3. The next attribute uses None to mark the end of the list. It is a sentinel value, telling the program that there are no more nodes to traverse and so errors are avoided when the code tries to access a next element.

4. While both store sequences . A Python list is a dynamic array ; elements are stored in contiguous memory , and access by index is fast . A linked list consists of nodes scattered throughout memory that reference each other, which makes it more efficient for inserting or deleting elements without having to reallocate the entire structure.

5. OOP is useful because it allows you to encapsulate data and the methods that manipulate that data into a single unit. The modularity makes it easier to organize , reuse , and understand complex structures such as linked lists and trees through clear relationships between objects .
